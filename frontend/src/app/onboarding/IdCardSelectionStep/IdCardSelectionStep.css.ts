import { appStyle } from "@/style/utils";

export const optionsFieldset = appStyle({
  border: 0,
  margin: 0,
  padding: 0,
});

export const disabledOptionTrigger = appStyle({
  outline: "none",
});
